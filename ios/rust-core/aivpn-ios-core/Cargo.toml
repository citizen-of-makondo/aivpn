[package]
name = "aivpn-ios-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "aivpn_ios_core"
crate-type = ["staticlib", "cdylib"]

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
